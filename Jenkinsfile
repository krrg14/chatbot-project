pipeline {
    agent any

    environment {
        withcredentials()
    }

    stages {
        stage('chech out') {
            steps{
                sh '''
                branch:main, url:
                '''
            }
        }
    }
}